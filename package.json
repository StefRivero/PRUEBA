{
  "name": "solotodo-playwright",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "testing": "QASE_MODE=testops npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.3",
    "playwright-qase-reporter": "^2.0.0"
  }
}
